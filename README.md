# paws.ovh

## Deployment

Set the environment variable `ADMIN_TOKEN` to a secret value before running the application. This token is required for upload and delete operations.

```bash
export ADMIN_TOKEN="your-secret-token"
```

## Design preview

Un aperçu du design moderne est disponible dans `public/preview.html`. Ouvrez ce fichier dans votre navigateur pour voir la maquette.
